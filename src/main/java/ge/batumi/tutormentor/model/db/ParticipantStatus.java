package ge.batumi.tutormentor.model.db;

/**
 * Status of a user's participation in a course: pending approval, active, or rejected.
 */
public enum ParticipantStatus {
    PENDING,
    ACTIVE,
    REJECTED
}
